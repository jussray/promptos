name: Issue Close Gate

on:
  issues:
    types: [closed]

permissions:
  contents: read
  issues: write

concurrency:
  group: issue-close-gate-${{ github.repository }}-${{ github.event.issue.number }}
  cancel-in-progress: false

jobs:
  enforce:
    if: ${{ github.event.issue.pull_request == null }}
    runs-on: ubuntu-latest
    steps:
      - name: Enforce founder-approved closure evidence
        uses: jussray/founder-control-room/.github/actions/issue-close-gate@3ecc57ca68094cb9baaf74b9f334e22b7cf44c62
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.issue.number }}
          closed-at: ${{ github.event.issue.closed_at }}
          repository-head: ${{ github.sha }}
          founder-login: jussray
